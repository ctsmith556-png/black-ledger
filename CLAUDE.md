# CLAUDE.md — Black Ledger

> Project context for Claude / Cowork sessions. Read this first, then open the specific docs it points to. Keep it updated as the project moves.
> **Place this file at the REPO ROOT** (`black-ledger/CLAUDE.md`) so any session opened in the project folder picks it up automatically.

---

## What this is

**Black Ledger** is a **single-player vehicular combat game** — Twisted Metal Black-inspired gameplay with a grim **Silent Hill / Se7en horror tone** (oppressive, psychological, not campy). Built by a **solo developer (Chris)** in **Unreal Engine 5.7**.

Framing: contestants with dark pasts are pulled into a deadly competition run by **"The Auditor."** Each vehicle is documented as an Auditor **case-file dossier** — that grim dossier look is the project's signature art style.

**Status (June 2026):** Pre-production complete; laptop set up; repo at `C:\Users\csmit\black-ledger` connected in Cowork. **All 16 Meshy vehicle models are committed** at `art/source/vehicles/` (FBX + PBR, ~1.7 GB LFS). **UE 5.7 project exists at the repo root** (`BlackLedger.uproject` + `Config/` + `Source/` + `Content/`, per `PROJECT_STRUCTURE.md`). The wizard produced a Blueprint project, so the C++ game module was added by hand. **Phase 1 complete (June 5, 2026): the Surgeon is drivable** — `ABLCombatVehicle` arcade pawn (raycast suspension, direct-yaw steering, handbrake drift, airborne auto-level), `UBLHealthComponent`, `ABLGameMode`, classic input bindings, and a scripted gym map `L_Gym`. **All 16 vehicles imported in-engine (June 8, 2026)** at `Content/BlackLedger/Characters/<Name>/` (body + 4 wheels + textures + materials) via the batch pipeline. **All 16 vehicles finalized + re-imported (June 10, 2026):** every wheel placed on **Blender-measured axle positions** (deterministic `--axle-fracs`), correct per-vehicle tire radii, and a **lateral wheel-track fix** so wheels seat in their wells instead of poking past the body. 15 of 16 are clean; **Bride is left on its older track by choice** (its cleaned source was reverted in a tooling mix-up, so re-prepping it would undo the manual blob/hood cleanup — its in-engine asset keeps the clean body, just slightly proud front wheels). The drivable pawn still spawns **only the Surgeon** (meshes + chassis numbers hard-coded); making the other 15 selectable is the next vehicle task (data-driven `UBLCharacterDataAsset`). Note: UBA is disabled on this laptop (`Saved/UnrealBuildTool/BuildConfiguration.xml`, machine-local) because its memory kill-threshold loops on a ~25 GB RAM machine.

---

## Core design facts

- **Roster:** 15 selectable launch characters + **Vault** (unlockable) = 16 launch vehicles. Plus **10 Collector bosses** that are overpowered and **become playable when defeated**. **The Auditor** is the final boss / ultimate unlock. **26 vehicles total.**
- **Each playable map has a boss fight** (challenging, Twisted-Metal style); beating a boss unlocks it.
- **10 arenas**, each with **2–3 destructible/interactive features** (Eiffel-Tower-collapse style).
- **14 pickup weapons.**
- **Stat budget:** Speed + Armor + Handling + Special Power = **24** for launch vehicles (bosses intentionally break this). Target time-to-kill ~**10–15s** standard.

**Launch vehicles (Ref_01–16):** 01 Surgeon, 02 Antoinette, 03 Hollow, 04 Warden, 05 Pup, 06 Crucible, 07 Cartographer, 08 Lien, 09 Hemlock *(eco-terrorism backstory)*, 10 Shepherd, 11 Specter, 12 Refuse, 13 Bride, 14 Hunter, 15 Photographer, 16 Vault *(unlockable)*.

**Bosses (Ref_B1–B10):** B1 Foundryman, B2 Curfew, B3 Condemned, B4 Jackknife, B5 Sexton, B6 Undertow, B7 Lodestone, B8 Headliner, B9 Alienist, B10 **Auditor** *(final)*.

**Arenas (10):** Ashen Hollow Downtown, The Tenements, Highway 6 Overpass, The Cemetery, The Auditor's Office, The Drowned Quarter, The Switchyard, Pinewood Drive-In, Saint Dymphna's (+ the opening arena). Details in the design bible.

**Weapons (14):** Homing Missile, Power Missile, Ricochet, Remote Bomb, Freeze Missile, EMP, Napalm, Spike Strip, Concussion Charge, Arc Lance, Process Server, Effigy, Final Notice, Health Pack.

---

## Tech & architecture

- **Engine:** Unreal Engine **5.7**. **C++ core + Blueprints.**
- **Toolchain:** Visual Studio **2026** (v14.44 toolset) is installed and **compiles UE 5.7 fine** — the earlier v143-only worry proved moot on this machine. "Game development with C++" workload required. VS 2026 also owns the `.uproject` double-click association (opens it as JSON); open the project via the UE Project Browser → Browse instead.
- **Vehicle pawn:** `ABLCombatVehicle` + components; data-driven via **Data Assets**.
- **Movement:** **arcade**, not sim. Preferred rig = body mesh + **4 wheel meshes as Blueprint components** (lighter than a full Chaos skeletal vehicle).
- **Destructibles:** runtime navmesh rebuild for collapsing geometry.
- **Source control:** **Git + Git LFS** (LFS for png/jpg/pdf/zip/docx/xlsx/fbx/glb/tga/wav/uasset/umap). GitHub free LFS = 10 GB.

---

## 3D art pipeline (Meshy → Blender → Unreal)

Tool: **Meshy Pro**. Per vehicle:
1. **Reference:** the dossier sheet + the multi-view prompt pack.
2. **Generate:** Meshy 6 **Image-to-3D / Multi-view** — 3 clean views (front/side/rear). Crops + prompts are in the multi-view kit.
3. **Remesh:** **Fixed** topology, **~60k tris** hero vehicles, **80–120k** bosses. (Faces are quads ≈ 2× tris in-engine.)
4. **Export:** FBX/GLB + PBR.
5. **Blender prep:** separate the 4 wheels, fix pivot (ground/center of wheelbase) and scale, clean hard-surface, cap the junk underside (don't detail undersides — chase cam never sees them).
6. **Unreal:** import, build materials; real text/badges as **decals**, not baked.
7. **VFX in Unreal, not baked into mesh:** Foundryman/Jackknife molten glow, Headliner lights, Auditor swirling papers.

**Done so far:** 16 vehicle models created in Meshy (the 16 launch vehicles).

---

## Where everything lives (docs/)

The detailed specs are the source of truth — open them as needed:

- **Design:** `DesignBible_v0.3.docx` (master), `BlackLedger_CombatTuning.xlsx`, `BALANCE_SIM.md`
- **Engineering:** `TECHNICAL_DESIGN.md`, `DAY_ONE_CHECKLIST.md`, `CONVENTIONS.md`, `PROJECT_STRUCTURE.md`, `SETUP.md`, `PREPRODUCTION.md`, `NAME_AND_TRADEMARK.md`, `INDEX.md`
- **Laptop setup:** `SETUP_NEW_LAPTOP.md` ← start here on a new machine
- **Art / 3D:** `art/MESHY_PLAYBOOK.md`, `MESHY_PROMPTS.md`, `MESHY_API_SETUP.md`, `meshy_batch.py`, `meshy_assets.json`
- **Reference sheets:** `vehicle-refs/` (26 dossiers — `png/` = latest art, `html/` = latest sheet format, `svg/` = legacy, + PDF), `weapon-refs/` (14)
- **Multi-view kit:** `MULTIVIEW_PROMPTS.md` + `multiview/<Vehicle>/` (front/side/top crops per vehicle)
- **Other:** `vertical-slice/`, `arenas/` (**`ARENA_INDEX.md` + 10 Arena Build Briefs** — authoritative for arena scale/tone/lanes/hazard timing; blockout svg/png stay authoritative for landmarks; read the brief before building or resizing any map), `vo/` (opponent barks), `audio/` (brief + event list), `ui/` (6 wireframes), `business/` (pitch, Steam, titles)
- **Raw 3D source art:** `art/source/vehicles/<NN_Name>/` at repo root (Meshy FBX + PBR textures, committed via LFS)

---

## Immediate next steps

1. **Phase 2 — combat core: COMPLETE (June 11).** All Week-2 pieces in C++: `ABLProjectile` + MG primary (`UBLWeaponComponent`, twin muzzles, placeholder tracers + muzzle-flash light), Homing Missile pickup chain (`ABLPickupActor` → pickup slot → `ABLProjectile_Homing`, 80/40 splash + knockback impulses), `UBLImpactFXSubsystem` (weight-scaled hit-stop, perlin camera shake w/ falloff, placeholder `ABLImpactBurst` flash+fireball, player-gated death slow-mo; audio + real Niagara = TODO hooks), `ABLAIController` (C++ state machine: pursue/strafe/peel/pickup/unstuck — BT version arrives with bosses), difficulty scaling per Bible 4.6 (`UBLGameInstance` Easy/Med/Hard: HP, fire duty cycle, aim cone, missile rate; menu UI later, `bl.SetDifficulty 0|1|2` now), TM:B-style 14-car matches (`ABLGameMode` ring spawn, `bl.NumAI` override, `bl.AIDebug 1` overlay), and speed+mass-scaled ram damage. Exit test = the 2-min brawl is fun → tag `phase2-combat`.
2. **Vehicle selection (data-driven) — the one remaining vehicle task, deferred by choice.** The pawn (`ABLCombatVehicle`) hard-codes the Surgeon's body/wheels + chassis numbers. To make all 16 drivable: add a `UBLCharacterDataAsset` (body + 4 wheel meshes + `AxleFrontX/RearX`, `TrackHalfY`, front/rear wheel radii, `BodyMeshZOffset`, mass, and the Speed/Armor/Handling/Special stat budget), give the pawn a `Character` property that applies it on spawn, and generate all 16 assets — the chassis numbers are already in each `*_prep_report.json` (wheelbase/track/radius) and the meshes are imported. Foundation for the character-select screen (`ui/`).
3. **Vertical slice (Phase 3) — IN PROGRESS (June 11). The #1 tech risk is RETIRED:** runtime navmesh rebuild on destructibles is proven in-engine. Built: `ABLNavBounds` (registers nav bounds in C++ — brush volumes can't be scripted; uses a protected-access shim for `AddNavigationBoundsUpdateRequest`), `ABLDestructibleFeature` base (health-driven, swaps authored navigable after-geometry, FX through the subsystem) + `ABLDestructible_Catwalk`, `RuntimeGeneration=Dynamic`, and `tools/unreal/make_mill_map.py` (L_Mill greybox per the blockout: furnaces F1–F3 + glow, loading bays, sealed bay, catwalk, pickups, nav bounds). Verified in PIE with `show Navigation`: collapse → tiles rebuild → green re-knits over the wreck ramps. Vehicles/projectiles/pickups/bursts are nav-irrelevant (`SetCanEverAffectNavigation(false)`) so 14 movable bodies don't churn tiles. **CRITICAL pipeline note: as of UE 5.7.4, scripted ACTOR SPAWNS under `-nullrhi` crash with EXCEPTION_INT_DIVIDE_BY_ZERO — run map-building scripts WITHOUT `-nullrhi` (asset imports are unaffected and keep it).** **Done June 12 (slice systems):** furnace pours on all 3 furnaces (`ABLHazardActor` telegraph/active cycle + blinking pour-pool disc); Power Missile (200/60, Massive) + Health Pack (33%, green crate, skip-at-full) complete the slice weapon subset; **special system** (`UBLSpecialComponent` cooldown/READY + `UBLSpecialAbility`; Surgeon's Operating Table = teal follow-field, ×2 incoming-damage mark via `IncomingDamageMultiplier` + 8 HP/s self-heal; input `BL_Special` Q/gamepad-X; AI pops it <14 m); **Foundryman in-engine at locked 18 m kaiju scale** — Meshy "Infernal Locomotive" (95k tris source, treads), `--treads` prep path (no carve; body-only; importer renames single-mesh `<Name>_UE`→`SM_<Name>_Body`), `ABLBossPawn` (subclasses the vehicle: 11,133 eff HP, armor 14, 12 t, 55 kph, hazard-immune via `bHazardImmune`, phases @66/33% w/ `OnPhaseChanged`, `RiseAt()` stub, `bl.SpawnBoss` cheat); **catwalk raised to 8.5 m deck** so the boss clears it (longer entry ramps, 11.4° wreck-ramps, ladle at z=1500). Vessel-nose-first facing is the locked boss orientation. **Done June 14 (front-end / menus bookend):** the whole out-of-match flow now exists in hand-rolled **C++ Slate** driven by `UBLUISubsystem` (a `UGameInstanceSubsystem` = the TDD §9 "UI manager on GameInstance"; no UMG/.uassets, so the menu layer compiles straight through Build.bat). Screens per `SCREEN_FLOW.md`: Main Menu (New Game / Continue / Roster / Options / Quit), Roster ("The Filing Cabinet"), Options (difficulty, persisted), Pause (Resume / Restart / Quit to Menu), Result (Victory / Defeat), Unlock (Foundryman claimed), and a **placeholder Ending still** (real motion-comic is the intro/ending bookend). **Roster dossier system (June 14):** the roster is now a 2-column grid of all 16 launch contestants (Surgeon = teal [OPEN], rest selectable), with Collector bosses listed under "CLAIMED COLLECTORS" only once unlocked (Foundryman). Selecting any file opens a **Biography page** — tagline + scrollable backstory pulled from the Design Bible — with **Watch Intro Cinematic** (shows that contestant's ~30s intro storyboard text as a placeholder until the real motion-comics exist), **Back**, and **Deploy** (Surgeon only; others show a SEALED / CLAIMED note). Bio/intro text lives in `Source/BlackLedger/UI/BLVehicleBios.h`, **auto-generated from `DesignBible_v0.3.docx` by `tools/gen_vehicle_bios.py`** (ASCII-sanitized; re-run to refresh). The `UBLUISubsystem` gained `ShowBiography`/`ShowIntro` + `BuildBiography`/grid `BuildRoster`. Wiring: `ABLMenuGameMode` + `make_mainmenu_map.py` build `L_MainMenu` (now `GameDefaultMap`; editor still starts in L_Gym); `ABLGameMode` raises the Result screen via the subsystem 1.6 s after a player/boss death (the canvas victory/defeat overlay was removed from `ABLHUD` — HUD now draws only live combat); `BL_Pause` (Esc / gamepad Special-Left) on the pawn toggles pause with `bExecuteWhenPaused` so it fires while the world is paused; `UBLSaveGame` extended (difficulty, last character, `bHasProgress` gates Continue) and the subsystem reads/writes the slot. **Done June 14 (intro/ending cinematics bookend):** a reusable **motion-comic player** in `UBLUISubsystem` (`PlayCinematic(beats, onComplete)` + `BuildCinematic`/`ShowCineBeat`/`CineNext`/`CineFinish`) shows storyboard text one beat at a time (sentence-split via `BLSplitBeats`), auto-advancing on a length-scaled timer with **Next / Skip** controls, then fires a completion callback. Wired into the flow: **Deploy** (Surgeon) now plays his ~30s intro → enters the Mill (`DeployContestant` stores `DeployedBio`); **Watch Intro Cinematic** on any dossier replays that contestant's intro → back to the bio; **Unlock → Continue** rolls the deployed contestant's ~60–90s ending → returns to menu (the old single-still `BuildEnding` placeholder was removed). The intro **and ending storyboards for all 16 launch contestants + the Foundryman are real text pulled from the bible** — `BLVehicleBios.h` now carries an `Ending` field too (regenerated by `tools/gen_vehicle_bios.py`). These are placeholder *text* motion-comics until real art/VO motion-comics are produced; the flow + pacing are done, so dropping in panel art later is a content swap, not a code change. **Done June 15 (FX/audio pass — first cut, procedural):** with no Niagara/sound assets in the project and the scripted no-uasset workflow, this pass is **procedural C++** (drive-approved over editor-authored Niagara). **FX:** scripted emissive materials (`tools/unreal/make_fx_materials.py` builds `M_BL_Emissive` unlit Emissive=Color×Strength + `M_BL_Smoke` unlit translucent via `MaterialEditingLibrary`); `ABLImpactBurst` upgraded to an emissive fireball + ballistic spark pool + a lingering translucent smoke puff + flash, weight-scaled (MG bursts kept cheap = 0 sparks since they fire ~10/s/car, missiles get the full set); `ABLProjectile` tracers swap to an emissive MID (weight-tinted) at BeginPlay. All FX **fall back to the engine basic-shape material** if the scripted mats aren't generated, so the build never depends on running the script. **Audio:** a code audio director mirroring `AUDIO_EVENTS.md` — `UBLAudioSubsystem` (WorldSubsystem) + `UBLAudioBank` (DataAsset of `USoundBase` slots). One-shots (MG fire/hit, missile launch/impact, power/explosion, vehicle death, special, pickup, UI), the engine loop (pitch/volume from speed), the low-HP heartbeat (rises as HP drops), and music states (Menu/Combat/BossP1-3/Victory/Death) that cross-fade. Wired: `UBLImpactFXSubsystem`→PostImpact, `UBLWeaponComponent`→fire/launch, `ABLCombatVehicle` Tick→engine+heartbeat & death, `ABLGameMode`→music (menu/combat/boss-rise/result), `ABLBossPawn::EnterPhase`→boss-phase music. Every call is **null-safe** — silent until the bank exists. **Placeholder-tone pipeline** so it's audible now: `tools/gen_placeholder_audio.py` synths 19 WAVs (stdlib) into `tools/audio_raw/`; `tools/unreal/import_audio.py` imports them as `USoundWave`, flags the loops, and builds `DA_BLAudioBank` with every slot wired. When Wwise/FMOD/real SFX arrive, the bank (or the subsystem's Play helpers) is the single swap point — call sites don't change. **Done June 15 (Foundryman fight + FX polish):** the Foundryman fight is now feature-complete for the slice. Already had: phases @66/33% (`OnBossDamaged`→`EnterPhase`), the mortar attack (telegraph ring→ballistic `ABLProjectile_Slag`), the molten wall (phase 2+), phase-3 furnace commandeering, the boss HP bar (`ABLHUD`), and phase music. **Added the missing payoff — the Phase-3 weak-point loop (Bible 4.6):** a glowing **Core** in the cage (emissive `M_BL_Emissive`) that idles as a dim ember; in phase 3 he **overheats** (speed 55→78 kph, `MaxSpeedKph` bump) and leaves a **fire trail** (periodic pure-visual `ABLImpactBurst`, no shake/audio); on a cadence he winds up a **slam** — the cage ramps **white-hot** as the tell + a ground telegraph ring — then a radial shockwave (damage + knockback, falloff), after which the **core is EXPOSED ~3 s** (`Health->IncomingDamageMultiplier`=3.0; HUD flashes "STRIKE THE CORE"). Bait the slam, punish the core. Death sequence: timers cleared, window closed, core goes dark, a final cupola detonation (unlock + result still driven by `ABLGameMode::NotifyVehicleDeath`). Tuning lives on `ABLBossPawn` UPROPERTYs (`SlamInterval/Telegraph/Range/Radius/Damage`, `ExposeSeconds`, `ExposedDamageMultiplier`, `OverheatSpeedKph`). **FX polish:** per-vehicle **damage smoke/fire** (`ABLCombatVehicle` gains a translucent `DamageSmoke` plume <55% HP + a flickering `DamageFire` ember <28% HP, both blazing on a wreck, desynced per car), and the **molten wall + furnace pour-pool now use the scripted emissive material** for a true glow (lit fallback if ungenerated). **Audio hooks** wired: `PostSpecial` (special activation) + `PostPickup` (weapon grab). **Done June 15 (tread-scroll):** the Foundryman now has **tread belts** down each flank (`TreadL`/`TreadR` cleated strip meshes on `ABLBossPawn`, since the inherited wheels are hidden) driven by a scripted scrolling-cleat material **`M_BL_Tread`** (built in `make_fx_materials.py` via `MaterialEditingLibrary`: `1 - abs(2*frac(U*Cleats + Scroll) - 1)` sharpened, dark→mid metal lerp + faint heat emissive). `UpdateTreads` advances the `Scroll` scalar each frame by forward speed × `TreadScrollScale`, so the cleats march when he advances and reverse when he backs up (lit-cube fallback if the material isn't generated). **Done June 15 (VO/bark architecture):** a drop-in voice-over system so real recordings wire in with no code change. `UBLVOBank` (DataAsset) holds per-character bark sets (`FBLCharacterVO`: Taunt/Hurt/Kill/LowHealth/Special/Death), per-boss roar sets (`FBLBossVO`: Rise/Phase2/Phase3/Attack/Death), and the Auditor's lines — each an **array** for random variation. `UBLAudioSubsystem` gained `PostBark`/`PostBossVO`/`PostAuditorLine` (+ `PlayVOSet`) with a shared **VO cooldown** so lines don't stack; every call is null-safe and, in non-shipping, **dev-prints `[VO] <event> (no line assigned)`** so the hooks are verifiable before anything is recorded. Wired: player **Taunt** on possession, **Special**, **LowHealth** (once per low spell), **Death** (keyed by `ABLCombatVehicle::CharacterKey`, default "Surgeon"); Foundryman **Rise/Phase/Attack/Death** roars; the **Auditor** opens each brawl. Import pipeline `tools/unreal/import_vo.py` builds `DA_BLVOBank` from `tools/vo_raw/{characters,bosses,auditor}/...` wavs (creates an empty bank for editor-assignment if none exist). Remaining for the slice: the rise-from-Furnace-3 cinematic (currently a teleport + quake) and **the actual VO recordings + final SFX** (the synth tones + the silent VO bank are placeholders; the systems are done). Mill lighting warm-up folded into the art pass. **Note (June 15): the Linux workspace mount went stale mid-session and mis-reported some source files as truncated; the authoritative file view + a compile-risk review confirmed all edits are intact. The build is the final check.** **Done June 16 (audio asset pass — first real SFX in):** kicked off replacing the synth placeholders with real **ElevenLabs-generated** SFX through the existing bank pipeline (no code/bank changes — `import_audio.py` swaps by filename). New helper tools: `tools/to_mono.py` (downmix stereo→mono — UE only fully 3D-spatializes mono sources, so all *positional* one-shots get mono'd) and `tools/trim_wav.py` (trim a one-shot to a max length with an 8 ms fade — e.g. clip a too-long MG shot so the 10 rounds/s don't smear). **Per-asset workflow:** generate on a paid ElevenLabs tier (commercial license, download **WAV**) → `to_mono.py` → `trim_wav.py` if needed → copy into `tools/audio_raw/` as the exact slot name (`mg_fire.wav`…) → run `import_audio.py`. **Status:** `mg_fire`, `mg_hit`, `missile_launch`, `missile_impact` are now **real (mono, 48 kHz)**; `power_impact` + `explosion` still synth placeholders, as are engine/heartbeat/special/pickup/UI + all music beds. **New planning docs (this session):** `docs/audio/AUDIO_ACQUISITION_PLAN.md` (SFX/VO/music sourcing strategy — Fab/Megascans, Sonniss, BOOM, AI-voice tools + the "perform-once" S2S trick, licensing/Steam-disclosure rules) and `docs/SLICE_ASSET_CHECKLIST.md` (the authoritative content manifest: every slice asset → import slot → source → license → priority → status; **work down by priority**). Loose ends the manifest flagged: `PostUISelect` hook exists but is never called; the Surgeon `Hurt`/`Kill` bark slots exist but aren't wired. **Fix (June 16, playtest):** positional one-shots had no attenuation, so distant AI machine-gun fire/hits played at full volume (and felt repetitive). `UBLAudioSubsystem` now builds a runtime `USoundAttenuation` in `Initialize` (sphere, 18 m full-volume inner radius → 90 m natural falloff) and `PlayAt` passes it + a ±8% random pitch per shot, so far-off combat fades and rapid hits vary. C++-only (mono sources from `to_mono.py` are what let it spatialize). **Mill dressing pass also landed (June 16):** `make_fx_materials.py` adds `M_BL_MillFloor`/`M_BL_MillWall` (dark concrete/metal), and `make_mill_map.py` assigns them + a guarded `PostProcessVolume` grade (desat/cold/bloom/vignette/grain) + thicker cold fog; the UE4 `fog_inscattering_color` name was fixed to try `fog_inscattering_luminance` first. Real Megascans meshes still swap in later. **Done June 16 (two-layer engine):** `UBLAudioBank` gained an `EngineRev` slot; `UBLAudioSubsystem::NotifyPlayerSpeed` now runs two attached looping layers — idle (`EngineLoop`) crossfades down as the rev layer (`EngineRev`) rises with speed, each pitched modestly within its band. Graceful fallback: with no `EngineRev` assigned it's the single-idle-plus-pitch behavior. `import_audio.py` has an `engine_rev` slot, so dropping `engine_rev.wav` in `tools/audio_raw/` wires it. (The Surgeon's real 21 s idle is in; a high-RPM rev loop is the remaining piece.) **Done June 17 (Mill lighting playability + weapon lights + green-circle fix):** the dark Mill was unplayable, so lighting was re-tuned to a **fire-lit foundry** in `make_mill_map.py`: removed the `SkyAtmosphere` (a daytime dome washed out the tone), each furnace now casts a bright orange **core + wide wash** point light, six scattered **molten fill** point lights spread orange across the floor, warm ember `ExponentialHeightFog`, a dim cold key light for form, and the `PostProcessVolume` **auto-exposure is locked** (min==max, slight +bias) so the scene reads as dark as lit instead of auto-brightening. **Weapon lights (dark-map visibility):** MG muzzle flash strengthened (`MuzzleFlashIntensity` 9k→20k, radius 700→1300) so each burst strobes the lane; `ABLProjectile` gained a `GlowLight` point light that's bright for heavy ordnance (missiles/Power, ember-tinted, weight-scaled) and dark for MG rounds (too many — they keep the bright emissive tracer). **Green-circle fix:** every vehicle currently defaults to the Surgeon's Operating Table special, so AI were popping it (teal `ABLDiagnosticField` discs over themselves every cooldown — the "green circles over the AI"). `UBLSpecialComponent::TryActivate` is now **gated to player-controlled pawns only** (temporary, until specials go data-driven per character); the player's own Q special is unchanged. **Foundryman treads — RESOLVED as STATIC (after a long detour):** the body is a single-material mesh whose tracks are modeled in but can't be UV-panned. Tried (a) procedural cube tread-strip overlays (`bShowTreadOverlays` on `ABLBossPawn`, auto-fit to body bounds) — read as a glowing comb; (b) splitting the tread faces into a 2nd material slot in Blender (`tools/blender/inspect_foundryman_treads.py` + `split_foundryman_treads.py`, `--tread-z 2.0 --keep-x-min -5.8` cleanly isolates the track band, cab excluded) then a scrolling `M_BL_FoundrymanTread` on slot 1 — **but `import_vehicle.py` kept importing the 2-material FBX as a single slot** (`inspect_foundryman.py` confirmed `1 material slot`), so it never took; (c) a single-slot masked-cleat rebuild of `M_Foundryman_Body` (`make_foundryman_treadmat.py`, LocalPosition-X cleats masked by local Z/X) — looked bad (orange). **Decision (drive): ship the Foundryman's own modeled treads STATIC for the slice; animated treads deferred to the boss art pass.** All the tread C++ on `ABLBossPawn` (BodyTreadMID, scroll on the body, the diagnostic) was removed; the cube-overlay path stays dormant behind `bShowTreadOverlays=false`. The four exploration scripts above + the unused `M_BL_FoundrymanTread` are left in `tools/` inert for a future revisit. Re-import the Foundryman to restore the clean textured body. **Mill structural meshes (IN PROGRESS):** first real Fab kit (LN3D **Modular Industrial Catwalk**, free) is wired into `make_mill_map.py` via a `MESHES` config dict + `dress_with_meshes()` (decorative no-collision overlays on the gameplay greybox); the catwalk `Line2x4` piece (200×400×170 cm, length on its Y) tiles along the mid-span at `yaw=90`. Still TODO: final catwalk height/length calibration, add the kit's stairs/supports, and wire the other downloaded Fab packs (industrial machine = furnace/vessel, propane tank, steel barrel, rusty scrap pile, vent pipe) into the empty `MESHES` roles. `M_BL_MillFloor`/`M_BL_MillWall` already darken the greybox until Megascans meshes replace it. **Done June 17 (Mill rehaul to the Build Plan — `Black_Ledger_The_Mill_Unreal_Map_Build_Plan.docx`; BUILT + PLAYED):** `make_mill_map.py` was rebuilt from a single open floor into the plan's **three combat loops** — a central kill floor with cover islands (4 `cover_column`s + rail-car LOS blockers + scrap, ~126 m clear central lane), an outer loading-bay recovery ring (5 bays + 6 rail cars + the sealed bay), and the elevated catwalk route — still at **520×440 m** (2× doctrine; route widths unchanged). **14 spawns distributed around the OUTER ring** (each clear of pours/cover ⇒ ≥2 exits, `BLSpawn`-tagged) and **12 pickups** (6 homing / 3 power / 3 health: center-offensive, ring-health, sealed-bay reward). Map stays **in place as `L_Mill`** (only one C++ ref — `BLUISubsystem.cpp` Deploy `OpenLevel`). **Distinct furnace pours (C++ — `ABLHazardActor`):** new `EBLPourShape {Circular, Fan, River}` + `PourYawDeg`/`FanHalfAngleDeg`/`RiverHalfWidth`/`RiverLength`; `IsInPourZone` shapes the damage volume and `ShapePourPool` shapes the pool visual (Circular disc / Fan ellipse-tongue / River cube-strip). Furnaces are now **A = fan (NW), B = river (NE, ~90 m line), C = circular (S = boss rise)**; Circular is the default so other hazards are unchanged. The Foundryman commandeers all three by **class iteration** (rename-safe), so phase 3 = relentless distinct pours. **Graybox-first per plan M1:** new `DRESS=False` flag skips the Fab mesh art pass (`dress_with_meshes`) — flip to True for the M5 art pass once the layout drives well; the June 16–17 catwalk/props dressing stays wired behind it. Verified before handoff: pour-shape geometry unit-tested + layout constraints (counts, wall/pour/cover clearances, river bound, 126 m central lane) asserted in a standalone harness; Chris built + playtested OK. **Still graybox placeholders (next = plan M3):** ladle release-pins and furnace-blow-opens-sealed-bay (catwalk collapse already works in C++). **Done June 18 (updated Build Plan v2 — escalation + M3 transformations + first real texture; BUILT + PLAYED):** the plan was revised with a "graybox-to-game-ready" production layer (sections 18–27, tables 10–14); we worked its gameplay levers in C++ + the texture pipeline. **(1) Phase-coupled furnace escalation** — `ABLBossPawn::EnterPhase` now ramps pour cadence on the boss phases: normal ~90s → **Phase 2 = 45s** (`CommandeerCycle(45,…)` on every furnace, "Tap works the mill against you") → Phase 3 = ~18s relentless (existing). **(2) M3 transformations** (reuse `ABLDestructibleFeature`): **`ABLDestructible_Ladle`** ("Tap the Ladle") — shoot the hanging ladle (500 HP = the "pins"), `OnCollapsed` deferred-spawns a recurring **circular slag hazard** in the centre + swaps the ladle to a low cover blob (weaponised centre); **`ABLDestructible_FurnaceValve`** ("Blow the Furnace", placed on Furnace B by the sealed bay) — shoot the valves (400 HP), `OnCollapsed` does a one-shot radial blast (lethal point-blank, boss hazard-immune) + drops any actor tagged **`BLSealedDoor`** (the sealed-bay inner wall, tagged in `make_mill_map.py`), opening the room + its power-missile reward; navmesh re-knits via the collision change. Catwalk collapse was already done ⇒ all three transformations in. **(3) Reusable flicker light** — `ABLIndustrialFlickerLight` (noise flicker + random brown-out, per-instance tunable); the map places 10 (5 sickly bay sodium lamps, 2 catwalk bulbs, 3 red furnace warning lamps). **(4) Texture pipeline (Pass 1 "Shell Replacement" start)** — `tools/unreal/import_textures.py` (data-driven `SETS`) imports a downloaded PBR set (base sRGB on; rough/normal linear; normal green auto-flipped for Poly Haven's OpenGL maps) and builds a **world-aligned** material (`WorldPosition.xy / TileCm` so a single huge floor cube tiles at real-world size). First set in: **Poly Haven "Concrete Floor Damaged 01" (CC0)** → `M_BL_MillFloorPBR`, now on the central floor + bays (looks great in-engine). `make_mill_map.py` prefers `M_BL_MillFloorPBR`/`M_BL_MillWallPBR`, falls back to the flat `M_BL_Mill*`. Raw sets drop in `tools/textures_raw/<folder>/`; licenses logged in **`CREDITS/ASSET_LICENSES.md`** (new). **Free-mesh sourcing beyond Fab:** Poly Haven (CC0, best fit), ambientCG (CC0 textures/decals), itch.io CC0 packs, Sketchfab (filter CC0/CC-BY — log attribution), BlenderKit; avoid CC-BY-NC/ND, editorial-only, and GrabCAD/CAD. **Deferred refinement:** normal-play furnace cadence still self-cycles per furnace (each ~90s, staggered ≈ a pour every ~30s) rather than the plan §23 Arena Manager's "one random furnace per 90s" — add the lightweight manager if the brawl feels too busy. Next downloads: a wall metal set (→ `M_BL_MillWallPBR`, script is ready) + rust/scorch decals. **Done June 18 (art pass cont. — boiler + walls + lighting; PLAYED):** **(1) Boiler mesh pipeline** — `tools/unreal/import_boiler.py` imports a Sketchfab **CC0 Industrial Boiler** (FBX, one mesh / three material slots: Decals / Objects / Kuma_Rusted Metal) via the proven classic-FBX path (Interchange off, `FbxImportUI`), builds a material per slot from the bundled PBR maps and **assigns by slot-name match** (`decal`/`object`/`rust`), saved as `SM_Mill_Boiler` under `/Game/BlackLedger/Meshes/industrial_boiler_01/`. Raw models drop in `tools/meshes_raw/<slug>/` (new folder + README). `make_mill_map.py` places **3 boilers centred on the furnace pits** (auto-fit ~20 m, solid collision; FC's is non-colliding so the Foundryman still rises). `place_static` gained a `collide` flag for big landmark meshes. **(2) Walls** — Poly Haven **Rusty Metal 04 (CC0)** → `M_BL_MillWallPBR`. **(3) Triplanar + matte materials** — `import_textures.py` gained a `projection` option: floors stay **world-XY** (flat plate), walls use **triplanar** (sample all 3 world planes, blend by |VertexNormalWS| — fixes the vertical smear/"shiny stripes" on walls + the round furnace platform) with matte constants (`rough_const`/`metallic_const`); world-XY path also supports `rough_min` clamp + `metallic_const`. The boiler materials got the same matte treatment (`ROUGH_MIN`/`METAL_SCALE` clamp roughness up + metallic down) — rusted iron, not chrome. **(4) Lighting rebalance to warm fire-lit** (the scene was reading cold blue): skylight dimmed + neutralised (blue 1.0 → near-neutral 0.45), directional "moonlight" halved + desaturated, furnace core/wash boosted (60k/30k → 95k/55k, wider radius), molten fills up, **white balance warmed 5200K → 7200K** (the sub-neutral value was itself bluing the frame), bloom 1.4 → 1.0 so pours read orange not white. New CC0 rows logged in `CREDITS/ASSET_LICENSES.md`. Tuning dials: `rough_const`/`tile_cm` (wall), `ROUGH_MIN`/`METAL_SCALE` (boiler), skylight intensity + `white_temp` (overall warmth). **Done June 18 (in-editor hand-finishing + decals):** decals placed by script (`place_decals` in `make_mill_map.py` — Megascans rust/blood/graffiti/road-debris, Fab Standard; floor decals use a THIN projection depth so they don't paint onto the vehicle; wall decals face by yaw E0/N90/W180/S270). Catwalk entry ramps reworked in C++ (`BLDestructible_Catwalk`): 9° gentle, end flush ~6 cm above the gantry edge (down-step, no wall), the lip-causing bridge piece removed. Catwalk support pillars centred under the deck (`MESHES["support"]["edge"]=0`). Scripted `rubble` role DISABLED (path ""). **CRITICAL — L_Mill now carries MANUAL EDITS the script only partly reproduces:** Chris hand-placed **4 rubble piles** (the rusty scrap mesh at **scale 0.8**, hand-rotated + grounded, e.g. one at −1816,−6089,122) and cleaned up the catwalk — **deleted 1 grate tile, deleted 2 overhead girders that floated in mid-air, and raised the remaining grate tiles to z=858**. Baked into the script as the closest fallback: catwalk `deck_z=858`, **girder role disabled** (path ""), rubble role disabled (hand-placed). The exact rubble transforms + which tile/girders were deleted live ONLY in the saved level. ⚠️ `make_mill_map.py` rebuilds L_Mill from scratch (`new_level`) and **WILL WIPE the hand-placed rubble + the exact catwalk cleanup** — the SAVED LEVEL is authoritative; do NOT re-run it without re-doing those by hand after. Material/texture/mesh-import scripts (`make_fx_materials.py`, `import_textures.py`, `import_boiler.py`) are SAFE — they touch `/Game/.../FX|Textures|Meshes`, not the level. To add new map actors going forward without wiping, use an ADDITIVE script (`load_level` + spawn + save), not `make_mill_map`. **Done June 22 (Mill rehaul to the reference concept art — square loop + centre furnace row + slag trench; BUILT + DRIVES WELL):** `make_mill_map.py` reworked to match the uploaded image: (1) the catwalk is now a **drivable SQUARE LOOP** — an elevated ring road (12 m wide, on columns) around the central pit with W/E entry ramps (greybox, flush 9° geometry), replacing the straight catwalk; (2) the **3 furnaces are a ROW across the centre** — FA left (fan) / FB centre (circular; ladle hangs above; **boss rises here**) / FC right (river); boilers centred on each (FB non-colliding for the rise); (3) a **sunken SLAG TRENCH** (E-W, 20 m wide, 4 m deep, ramp down at each end, retaining walls + lamps) — the floor is built as a 4-plate FRAME around the opening, so all floor pieces are now `Floor_*` and the material loop matches `lbl.startswith("Floor")`; (4) the C++ `BLDestructible_Catwalk` is kept as a **collapsible cross-bridge** (E-W at y=8000); (5) the furnace-breach valve moved to **FC** (nearest the E-wall sealed bay); 12 pickups + 14 spawns re-fit around the loop/ring. Geometry unit-checked (ramp-flush, frame seams, clearances) + Chris drive-tested OK. ⚠️ **The earlier straight-catwalk / NW-NE-S-furnace geometry is SUPERSEDED** — ignore those coordinates. Rubble is still **hand-placed** (re-done by Chris after this rebuild); re-placing it by hand stays the last step after any `make_mill_map` re-run. **Done June 22 (cont. — catwalk-as-south-loop + slag trench + furnace swap + bugfixes; DRIVES WELL):** (1) the collapsible C++ catwalk is now the **MIDDLE of the SOUTH loop side** (not a floating mid-map bridge) — added a `bEntryRamps` UPROPERTY to `ABLDestructible_Catwalk` (Python name `entry_ramps`; when false, BeginPlay hides its own floor entry ramps) so it's a flush deck section reached by driving the loop; greybox road flanks it out to the corners; shooting its supports collapses the mid-span into a debris ramp down to the floor (matches the blockout's "collapsible catwalk → debris ramp"). (2) Furnace pours swapped to the blockout diagram: **B (centre) = straight river, C (right) = circular** (A left = fan unchanged). (3) The **lower slag trench** (escape/ambush route, plan §3) is in — a sunken E-W channel (20 m wide, 4 m deep); the floor is built as a 4-plate FRAME around the opening (all `Floor_*`), with retaining walls and **THICK (1.5 m) gentle ~6.6° end ramps** (thin ramps tunneled at speed and felt like a wall). (4) Orphaned old-straight-catwalk dressing tiles (next to the furnaces) disabled (`MESHES["catwalk"]`/`["support"]` paths set to ""). **⚠️ Bug lesson:** a C++ bool `UPROPERTY bXxx` is exposed to Python as `xxx` (drop the leading 'b') — calling `set_editor_property("b_entry_ramps", ...)` THREW and aborted the map build mid-way, which saved a **BLANK L_Mill** (endless-fall in PIE). The call is now name-tolerant + guarded so a property typo can never blank the level again. **NEW additive-edit pattern:** `tools/unreal/fix_trench_ramps.py` loads L_Mill, edits actors by label, and saves — WITHOUT `new_level`, so it **preserves the hand-placed rubble**. Use this pattern (NOT `make_mill_map`) for small in-place L_Mill tweaks so Chris doesn't re-place rubble every time. **Mill status:** structurally + visually most of the way to the reference; remaining to finalize = a **systems re-verify + combat-tuning pass** (confirm ladle/furnace-breach/catwalk-collapse/boss all fire on the new layout; tune furnace cadence, pours, pickup flow vs plan §15), then optional M6 atmosphere (ambience bed + memorial prop via additive script). After that, the natural pivot is **vehicle selection** (data-driven `UBLCharacterDataAsset`) to make the other 15 cars drivable. **FREE-ASSET SOURCING (Chris's pipeline; all CC0 / commercial-OK, log each in `CREDITS/ASSET_LICENSES.md`):** **Textures** = Poly Haven (CC0) → drop a downloaded ZIP's maps in `tools/textures_raw/<slug>/`, run `import_textures.py` (floors world-XY, walls triplanar+matte). **Decals** = Megascans via **Fab** (free, Fab Standard) added in-editor through the **Fab / Quixel Bridge plugin** (Window → Fab → sign in → filter Free), land in `/Game/Fab/Megascans/Decals/`, placed by `make_mill_map.py place_decals`; ambientCG (CC0) is the backup → `tools/decals_raw/`. **Meshes** = Fab (Megascans + free packs, Fab Standard) and Sketchfab (filter **CC0**/CC-BY, log attribution) → raw FBX/glTF + textures in `tools/meshes_raw/<slug>/`, import via a per-asset script (pattern: `import_boiler.py`). Avoid CC-BY-NC / CC-BY-ND, editorial-only, and GrabCAD/CAD. *(Menu UI notes: pawn-data-driven roster selection is still deferred so only the Surgeon actually launches a match; claimed Collectors show but aren't drivable until `UBLCharacterDataAsset` lands. Esc-to-resume from the pause screen is via the Resume button — the screen runs in UI-only input. **Input-mode gotcha (fixed June 15):** the menu/intro cinematic run in `FInputModeUIOnly`, which puts the **GameViewportClient** (it survives `OpenLevel` into the arena) into ignore-game-input / no-mouse-capture; nothing reset it on match start, so the car wouldn't drive after Deploy. Fix: `ABLCombatVehicle::SetupPlayerInputComponent` forces `FInputModeGameOnly` + hides the cursor on possession. Any new front-end screen that leaves UI-only input relies on this restore when gameplay resumes. The dossier-styled UMG art pass can later replace the Slate look without touching the flow logic, which all lives in `UBLUISubsystem`.)*

**Known refinements deferred:** vehicle selection goes data-driven later (step 2) — the pawn is Surgeon-only for now, which is fine for combat work. **AI on large maps:** (a) RESOLVED June 12 — `ABLAIController` now has `EngagementRange` (160 m, difficulty-scaled) with sticky-target hysteresis, a ROAM state (wanders toward pickup spawns 70% of the time = traffic through conflict lanes), and voluntary detours (every ~15 s, 30% chance to break off for 8–14 s; only point-blank threats interrupt; `bl.AIDebug` shows ROAM green / DETOUR white / ENGAGE orange); (b) still deferred: steering is straight-line + unstuck only — real arenas need the TDD §8 NavMesh/Detour + AIPerception driving, which lands with the arena content work. Explosion VFX are placeholder (orange burst) by choice — real Niagara explosions/tracers/impacts come in the vertical-slice FX pass. **Map scale doctrine (June 12, drive-approved): ALL arenas build at 2× the Arena Build Brief footprints** — fights stay frequent but players can free-roam and disengage; briefs stay authoritative for proportions/lanes/tone (lane widths do NOT scale — they fit the vehicles); see `docs/arenas/ARENA_INDEX.md` for the per-arena build sizes + coupled systems to revisit (AI dispersion, range constants, navmesh invokers on the ~1 km maps). L_Mill generates at **520×440 m** (`SCALE = 2.0` in `make_mill_map.py`): 12 m catwalk lane, 12+3 pickups in conflict lanes, 14 `BLSpawn`-tagged FFA spawn nodes (`ABLGameMode` prefers tagged nodes, ring fallback otherwise), ladle landmark, 40 m backdrop apron, and **pour hazards on all three furnaces** (~20/65/90 s staggered; Foundryman commandeers F3 on arrival). **Bride** carries the older wheel track (clean body, slightly proud front wheels); its cleaned source was reverted in a tooling mix-up, so its re-prep was skipped by choice — re-clean + re-prep if a perfect Bride is wanted before launch. The six-wheelers' middle wheels are cosmetic/static, merged into the body; split them out for spin when the pawn goes data-driven.

**Vehicle pipeline (proven, all 16 done):**
- `tools/blender/prep_vehicle.py` (host Blender, headless) — carves fused Meshy wheels, installs cylinder combat wheels + dark wheel-well liners, real-world scale, ground/wheelbase pivot, decimate, UE-ready FBX + preview renders. Per-vehicle flags: `--length`, `--flip`, `--wheel-radius`, `--wheel-width`, `--decimate`, plus **`--width` / `--height`** (non-uniform scale, e.g. Shepherd) and **`--front-axle-x` / `--rear-axle-x` / `--wheel-y`** (pin an axle when auto-detect is unreliable). Axle consolidation lives in `tools/blender/wheel_solve.py` (pure, bpy-free, unit-tested by `test_wheel_solve.py` — run `python tools/blender/test_wheel_solve.py`); **a weak axle now auto-repairs by mirroring the reliable one**, which fixes narrow-nosed bodies (Bride) where the detector found no front geometry. **`--mid-axle-x <X[,X]>`** installs extra cosmetic axles for the six-wheelers (`build_extra_axles` in `wheel_solve.py`); they're carved/lined like the corners (also clearing the fused original wheel) and merged into the body, so they import with no pawn change. **`--axles N`** (N≥2) swaps the front/rear circle-fit for k-means clustering of the wheel geometry into N axles — the robust path for tandem/3-axle trucks (Crucible, Hemlock, Lien, Refuse, Shepherd, Bride); outer two axles become the spinning corners, inner axles merge into the body. The full-res QA re-audit (`VEHICLE_QA_REVIEW.md`) found the original contact-sheet pass under-caught these six; the other 10 are genuine 2-axle bodies and stay on the classic path.
- **Wheel placement (final method, all 16 — supersedes the auto-detect paths above):** positions are **measured in Blender, not detected.** `tools/blender/measure_axles.py` — import the vehicle's *source* FBX (original wheels still in their wells), drop one Empty on each wheel center in side view, run it; it prints `--axle-fracs` as fractions of length (frame-independent, so flip never matters) and copies them to the clipboard. Each vehicle's measured fracs live in `batch_prep.ps1`. `build_axles_at` places an axle at each frac, RANSAC-fits the tire for radius, and **derives the lateral track from the tire's own lower arc** (where no fender reaches) so the wheel's outer face meets the tire edge instead of poking past the body — the **June 10 stick-out fix.** Per-vehicle overrides for bodies that fool the auto-fit: **`--wheel-radius`** (Antoinette/Cartographer 0.322, Photographer 0.311, Specter 0.45, Shepherd 0.52, Bride 0.40) and **`--wheel-y`** track pins (Antoinette 0.80, Photographer 0.70, Vault 1.30). `tools/blender/export_clean.py` re-exports a hand-cleaned mesh to a vehicle's source FBX via a dropdown picker — **save the .blend before exporting.**
- `tools/unreal/import_vehicle.py` (headless UE) — wipes the dest folder, imports meshes/textures (importer-agnostic: strips Interchange's `<Name>_UE_` prefix), builds + assigns materials, saves. Run via `-ExecCmds="py …"` with env vars `BL_VEHICLE_NAME`/`BL_VEHICLE_FOLDER` — **not** `-run=pythonscript` (Slate crash). **The post-save teardown "Fatal error" under `-nullrhi` is known/harmless — check the log for `IMPORT OK` and the Content folder on disk for truth.**
- **Batch runners:** `tools/blender/batch_prep.ps1` + `tools/unreal/batch_import.ps1` (data-driven roster table: name/folder/length/flip; `-Only <Name>,<Name>` runs a subset). Run with `powershell -ExecutionPolicy Bypass -File <script>`. **Editor must be CLOSED for any UE import** (asset locks); Blender prep is fine with it open.

*(Done June 4: docs pushed; tools installed; repo cloned + Cowork connected; `vehicle-refs updated/` folded in; 16 Meshy models committed via LFS; UE project at repo root with hand-added C++ module. Done June 5: Phase 1 drivable Surgeon. Done June 8: all 15 remaining vehicles batch-prepped + imported in-engine; full QA audit of all 16 (`docs/VEHICLE_QA_REVIEW.md`) — facing verified, 14 clean; prep pipeline hardened (axle auto-repair + manual axle pins + non-uniform scale, with `wheel_solve.py` unit tests); Bride front-axle fix, Shepherd bus-rescale, and opt-in 3rd-axle support (`--mid-axle-x`) for the six-wheelers staged for re-prep. Done June 10: **all 16 vehicles finalized** — every wheel moved onto Blender-measured axle positions (`measure_axles.py` → deterministic `--axle-fracs`, replacing auto-detect), per-vehicle tire radii corrected, and a **lateral wheel-track fix** added to `build_axles_at` (track read from the tire's lower arc) so wheels seat in their wells; all 15 non-Bride re-prepped + re-imported, Bride left on its older track by choice (reverted source). Pawn still Surgeon-only — data-driven selection deferred. Done June 11: **Phase 2 combat pieces 1–2** — `ABLProjectile` + machine-gun primary via `UBLWeaponComponent` (6 dmg × 10 rds/s = the 60-DPS primary-only profile), Homing Missile pickup (`ABLPickupActor` + `ABLProjectile_Homing`, 80 direct / 40 splash per the tuning sheet) built and play-tested in `L_Gym`; inputs `BL_Fire` (LMB/Space/RB) + `BL_FirePickup` (RMB/E/LB). Found + fixed a **wheel-pivot import bug**: UE's FBX import bakes node transforms into verts (`transform_vertex_to_absolute` — must stay default True; False also skips the Y-up→Z-up conversion and meshes import sideways), so wheels exported at their axle positions got ~2 m pivot offsets and orbited the vehicle when spun. Fix: `prep_vehicle.py` now parks wheel objects at the world origin before export (axle positions live in the prep report + pawn chassis numbers, not the FBX). Surgeon re-prepped + re-imported clean; pawn chassis numbers corrected to the prepped frame (axles **±177.78** — prep pivot is wheelbase center, so symmetric — track 100.78, radii 39.2). Remaining 14 non-Bride vehicles re-prepped + re-imported with the fixed pipeline (their June-10 assets carried the same baked wheel offsets). Bride still excluded (reverted source; its wheels also carry the orbit bug, which only matters once it becomes drivable — hand re-clean via `export_clean.py`, then re-prep + re-import, remains the gate).)*

---

## How to work with this project

- **Chris prefers concise, direct answers** — minimal preamble and verbosity.
- Produce **real files** saved to the connected folder (not just chat text); verify outputs (render/preview, contact sheets, etc.); track work with a task list.
- Keep the **grim dossier / horror tone** consistent in all art and copy.
- Naming: `Ref_NN_Name` (launch), `Ref_BN_Name` (boss), `Wref_NN_Name` (weapon).
- When resuming on a new machine: read this file, then `SETUP_NEW_LAPTOP.md`, then `DAY_ONE_CHECKLIST.md`.
